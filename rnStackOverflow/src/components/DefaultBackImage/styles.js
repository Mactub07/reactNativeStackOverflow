import { StyleSheet } from 'react-native';
import { constants } from '../../constants/index';

const styles = StyleSheet.create({ headerLeftBackButton: { marginHorizontal: constants.MARGIN_BASIC / 2 } });

export default styles;
